# Solo hace falta en producción, cuando el API vive en otro dominio.
# En desarrollo se deja vacío: Vite hace proxy de /api al servidor Express.
# VITE_API_URL=https://api.tudominio.com/api

# Destino del proxy en desarrollo (por defecto http://localhost:4000)
# VITE_API_PROXY=http://localhost:4000
